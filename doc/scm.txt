////////////////////////////////////////////////////////////////
// SCM
////////////////////////////////////////////////////////////////]

# Init git repo

echo "# KOTLIN_DEMO" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/aperezNWO/KOTLIN_DEMO.git
git push -u origin main

C:\Program Files\Eclipse Adoptium\jdk-21.0.12.8-hotspot\bin\

"CANNOT LOCATE JAVA INSTALLATION IN SPECIFIED JDKHOME"
C:\Program Files\Eclipse Adoptium\jdk-21.0.12.8-hotspot
¿Do you want to use the default version?

(powershell)
$env:JAVA_HOME="C:\Program Files\Eclipse Adoptium\jdk-21.0.12.8-hotspot"
echo $env:JAVA_HOME


$env:JDK_HOME="C:\Program Files\Eclipse Adoptium\jdk-21.0.12.8-hotspot"
echo $env:JDK_HOME

java -version

#¤ Install Spring Boot CLI if you don't have it, or just use curl:
curl https://start.spring.io/starter.tgz \
  -d type=gradle-project \
  -d language=kotlin \
  -d bootVersion=3.3.4 \
  -d groupId=com.example \
  -d artifactId=ping-api \
  -d name=ping-api \
  -d description="Zero KB ping api" \
  -d packageName=com.example.pingapi \
  -d dependencies=web \
  -d javaVersion=21 | tar -xzvf -

¤¤ run locally (command prompt)
gradlew.bat bootRun
curl -i http://localhost:8085/ping
curl -i http://localhost:8085/api/data/getAllPersons 
curl -i http://localhost:8085/api/data/getAllLogs 

¤¤ build docker image
  ¤¤ install / init docker desktop 
  ¤¤ allow visual studio code extensions
  ¤¤ create Dockerfile
  ¤¤ create render.yaml

# 0. in case required go to docker hub to get a key
docker login 

# 1. Rebuild the image with your changes
docker build -t ping-api .

# 2. Tag it (same command as before)
docker tag ping-api apereznwogmail/ping-api:latest

# 3. Push to Docker Hub (no need to login again unless session expired)
docker push apereznwogmail/ping-api:latest

¤¤ check docker hub
https://hub.docker.com/repositories/apereznwogmail

¤¤ creaete render web service
docker.io/apereznwogmail/ping-api:latest
https://ping-api-latest.onrender.com
curl.exe -i https://ping-api-latest.onrender.com/ping

¤¤ create cron-job to override render free account delay behaviour

https://ping-api-latest.onrender.com


///////////////////////////////////////////////////////////

## TESTING END POINTS 

## run server   
gradlew.bat bootRun

####################################################################
## LOCAL TESTING
####################################################################

# Ping endpoint (other window)
curl -i http://localhost:8080/ping

# Mandelbrot (default bounds)
curl -i "http://localhost:8080/api/fractals/generate?kind=1"

# Julia with custom bounds
curl -i "http://localhost:8080/api/fractals/generate?kind=2&xMin=-1.5&xMax=1.5&yMin=-1.5&yMax=1.5&maxIterations=500"

# Barnsley Fern
curl -i "http://localhost:8080/api/fractals/generate?kind=3"

## DIJKSTRA
curl -i http://localhost:8080/GenerateRandomVertex_SpringBoot

## GET ALL PERSONS 
curl -i http://localhost:8080/api/data/getAllPersons

## GET ALL LOGS 
curl -i http://localhost:8080/api/data/getAllLogs

####################################################################
## REMOTE TESTING
####################################################################

# Ping endpoint (other window)
curl -i https://ping-api-latest.onrender.com/ping

# Mandelbrot (default bounds)
curl -i "https://ping-api-latest.onrender.com/api/fractals/generate?kind=1"

# Julia with custom bounds
curl -i "https://ping-api-latest.onrender.com/api/fractals/generate?kind=2&xMin=-1.5&xMax=1.5&yMin=-1.5&yMax=1.5&maxIterations=500"

# Barnsley Fern
curl -i "https://ping-api-latest.onrender.com/api/fractals/generate?kind=3"

# Dijkstra
curl -i "https://ping-api-latest.onrender.com/GenerateRandomVertex_SpringBoot"

## GET ALL PERSONS 
curl -i https://ping-api-latest.onrender.com/api/data/getAllPersons

## GET ALL LOGS 
curl -i https://ping-api-latest.onrender.com/api/data/getAllLogs

///////////////////////////////////////////////////////////