# Init git repo

echo "# KOTLIN_DEMO" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/aperezNWO/KOTLIN_DEMO.git
git push -u origin main


#¤ Install Spring Boot CLI if you don't have it, or just use curl:
curl https://start.spring.io/starter.tgz \
  -d type=gradle-project \
  -d language=kotlin \
  -d bootVersion=3.3.4 \
  -d groupId=com.example \
  -d artifactId=ping-api \
  -d name=ping-api \
  -d description="Zero KB ping api" \
  -d packageName=com.example.pingapi \
  -d dependencies=web \
  -d javaVersion=21 | tar -xzvf -

¤¤ run locally
gradlew.bat bootRun
curl -i http://localhost:8080/ping


¤¤ build docker image
  ¤¤ install / init docker desktop 
  ¤¤ allow visual studio code extensions
  ¤¤ create Dockerfile
  ¤¤ create render.yaml

docker build -t ping-api .
docker tag ping-api apereznwogmail/ping-api:latest
docker login 
docker push apereznwogmail/ping-api:latest

¤¤ check docker hub
https://hub.docker.com/repositories/apereznwogmail

¤¤ creaete render web service
docker.io/apereznwogmail/ping-api:latest
https://ping-api-latest.onrender.com
curl.exe -i https://ping-api-latest.onrender.com/ping

¤¤ create cron-job to override render free account delay behaviour

https://ping-api-latest.onrender.com
